https://10.81.6.4 {
    tls /certs/cert.pem /certs/key.pem
    reverse_proxy web:8000
    encode gzip
    
    log {
        output file /var/log/caddy/access.log
    }
}

# HTTP 重定向到 HTTPS
http://10.81.6.4 {
    redir https://10.81.6.4{uri} permanent
}
